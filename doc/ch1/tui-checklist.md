# MewCode Claude Code 风格 TUI Checklist

> 本次改造于 2026-09-06 验收；真实密钥仅保留在未提交的 `mewcode.yaml`。

## 自动化验收

- [x] 欢迎页：`uv run pytest tests/test_tui.py -k layout` 验证小猫、MewCode、模型，并确认不显示固定示例提示。
- [x] 首轮切换：验证 `Enter` 后移除欢迎页，显示用户 `›` 提示符、助手 Markdown 与状态更新。
- [x] 多行输入：验证 `Shift+Enter` 插入换行、`Enter` 提交。
- [x] 思考区：验证 thinking 默认折叠且可展开。
- [x] 思考入口位置：验证折叠入口位于正式回答之后，长回答自动跟随时仍留在视口底部附近。
- [x] 活动提示：验证请求发出后立即显示“正在思考…”，首段正式回答到达后自动隐藏。
- [x] 错误与取消：验证生成结束后恢复输入，错误可见，`Ctrl+C` 可取消。
- [x] 多轮历史：验证第二轮 Provider 请求带有前一轮用户和助手消息。
- [x] 流式跟随：模拟超过视口高度的分段回复；即使手动滚到顶部，后续片段到达后仍自动滚至底部。
- [x] 全量回归：`uv run pytest` 通过 39 项；`uv build` 成功。

## tmux 端到端验收

- [x] 启动：通过 tmux + winpty 以有效 Claude 配置启动，显示小猫欢迎页、目录、`anthropic / claude-sonnet-4-6` 和底部状态行。
- [x] 第一轮：发送“请只用两个词回答：蓝天”，收到流式回复“白云”，状态回到准备就绪，消息计数为 2。
- [x] 上下文：追问“刚才的回答是什么？”，模型回复“白云”，消息计数为 4，证明历史已发送。
- [x] 退出：发送 `/exit` 后 TUI 返回 shell。
- [x] 活动状态：发送 Python 生成器问题后，回复到达前显示“◌ 正在思考…”，回复完成后提示消失且状态恢复为准备就绪。
